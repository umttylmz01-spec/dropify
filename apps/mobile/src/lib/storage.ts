export async function saveLocal(key: string, value: any) {
  // Placeholder: implement AsyncStorage or SecureStore as needed
  return { ok: true };
}

export async function loadLocal(key: string) {
  return { ok: true, value: null };
}
