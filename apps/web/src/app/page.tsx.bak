"use client";

import { TopBar } from "@/components/TopBar";

export default function Home() {
  return (
    <>
      <TopBar />
      <main style={{ padding: 24 }}>
        <h1 style={{ fontSize: 22, fontWeight: 700 }}>Dropify Web</h1>
        <p style={{ marginTop: 8 }}>
          Builder iskeleti: /new → proje oluştur, /project/[id] → spec + generate, /support → destek.
        </p>
      </main>
    </>
  );
}
